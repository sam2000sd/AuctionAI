# Auction Intelligence Clean Rebuild, Fixed Launcher

This version fixes the broken `.venv` issue.

Use:
1. Double-click `Start_Auction_Intelligence_VISIBLE.bat` first.
2. The launcher will delete/rebuild a broken `.venv` automatically.
3. After it works, use `Start_Auction_Intelligence_Hidden.vbs`.

If your previous folder still has a bad `.venv`, this launcher should repair it automatically.
